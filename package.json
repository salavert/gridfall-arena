{
  "name": "gridfall-arena",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "A neon procedural arena brawler where runners harvest cores and survive a collapsing grid.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "npm test && npm run build",
    "portrait": "node scripts/render-volt.mjs",
    "character:inspect": "node scripts/inspect-character.mjs",
    "character:install-sidekick": "node scripts/install-sidekick-local.mjs"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "vite": "7.3.6"
  },
  "license": "MIT"
}
