{
  "name": "gridfall-arena",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A neon procedural arena brawler where runners harvest cores and survive a collapsing grid.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "vite": "7.3.6"
  },
  "license": "MIT"
}
